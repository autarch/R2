<div id="summary">
  <h1><& SELF:title, %ARGS &></h1>
</div>

<h2>
Found <% $search->contact_count() %> matching contacts
</h2>

<table>

  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
    </tr>
  </thead>

  <tbody>
% while ( my $contact = $contacts->next() ) {
    <tr>
      <td>
        <a href="<% $c->uri_for( '/contact/' . $contact->contact_id() ) %>">
%   if ( $contact->is_person() ) {
        <% $contact->person()->full_name() %>
%   } elsif ( $contact->is_household() ) {
        <% $contact->household()->name() %>
%   } else {
        <% $contact->organization()->name() %>
%   }
        </a>
      </td>
      <td><% $contact->preferred_email_address()->email_address() %>
    </tr>
% }
  </tbody>

  <tfoot>
  </tfoot>

</table>

<%args>
$search
</%args>

<%init>
my $contacts = $search->contacts();
</%init>

<%method title>
Search
<%args>
$search
</%args>
</%method>
