<div id="summary">
  <h1>
   <& SELF:title, %ARGS &>
  </h1>
</div>

<table class="standard-table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Username</th>
      <th>Added</th>
      <th>Permissions</th>
    </tr>
  </thead>
% while ( my ( $user, $role ) = $users->next() ) {
  <tbody>
    <tr>
      <td><% $user->person()->display_name() %></td>
      <td><% $user->username() %></td>
      <td><% $c->user()->format_date( $user->creation_datetime() ) %></td>
      <td><% $role->name() %></td>
    </tr>
  </tbody>
% }
</table>

<p>
  <a href="<% $account->uri( view => 'new_user_form' ) %>">Add a user</a>.
</p>

<%args>
$account
</%args>

<%init>
my $users = $account->users_with_roles();
</%init>

<%method title>
<% $account->name() %> Users
<%args>
$account
</%args>
</%method>
